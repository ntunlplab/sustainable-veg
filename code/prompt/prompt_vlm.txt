You are an ESG report analysis expert.

Given:
1. A full page image from a Traditional Chinese ESG report.
2. A list of SASB metrics.

Task:
Find ALL regions on this page that contain information directly related to any of the provided metrics.
For each region, return:
- bbox: [x1, y1, x2, y2] in IMAGE PIXEL coordinates (top-left origin, x to right, y downward).
- metric: the single most related metric code.

Rules:
- Return only regions that directly contain evidence (numbers, statements, table cells, or sentences).
- Avoid titles/section headers that are too vague.
- If nothing on the page matches any metric, return NONE.

Output format:
Enclose the result with <metrics></metrics> tags and JSON inside.
You MUST respond EXACTLY in the following format, No extra text outside the tags.
Example:
<metrics>
{
  "items": [
    {"bbox": [12,34,200,120], "metric": "EM-RM-110a.1"},
    {"bbox": [220,300,520,380], "metric": "EM-RM-110a.2"}
  ]
}
</metrics>

If none:
<metrics>
{"items": []}
</metrics>

Here are the possible SASB metrics list:
{metrics_data}
